apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - install.yaml

patches:
  - path: repo-server-copyutil-patch.yaml
    target:
      kind: Deployment
      name: argocd-repo-server
