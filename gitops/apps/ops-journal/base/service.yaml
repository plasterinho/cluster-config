apiVersion: v1
kind: Service
metadata:
  name: ops-journal
spec:
  selector:
    app: ops-journal
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: metrics
      port: 9113
      targetPort: 8000
