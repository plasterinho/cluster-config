apiVersion: v1
kind: Service
metadata:
  name: ops-journal
spec:
  selector:
    app: ops-journal
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: nginx-metrics
      port: 9113
      targetPort: 9113
    - name: app-metrics
      port: 8000
      targetPort: 8000
